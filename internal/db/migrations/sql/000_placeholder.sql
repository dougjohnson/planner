-- Placeholder migration to bootstrap the migration runner.
-- Real schema migrations (001+) will be added by subsequent beads.
-- This file ensures the embedded filesystem has content at compile time.
SELECT 1;
