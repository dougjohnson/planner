## Configuration

Set up the config file:

```yaml
## This heading inside a code block should NOT split
server:
  port: 8080
  host: 127.0.0.1
```

## Implementation

The implementation uses the config:

```go
## Another fake heading in code
func main() {
    fmt.Println("hello")
}
```

Additional notes after code block.

## Summary

Everything works correctly.
